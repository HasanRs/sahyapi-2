# Postgres (Prisma) — Şah Yapı own database (no Atamep Firebase)
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Optional Google Analytics (do not reuse Atamep property)
# NEXT_PUBLIC_GA_ID=
